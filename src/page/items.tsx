import { useProjectContext } from "../data/model/project";

export default function ItemsPage() {
    const [project, setProject] = useProjectContext();

    return (
        <div className="">
            
        </div>
    );
}
